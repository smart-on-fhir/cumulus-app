import { Options } from "highcharts";

export const options: Options = {
    chart: {
        type: "areaspline",
        options3d: {
            enabled: false
        }
    }
}

export default options